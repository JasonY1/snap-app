<%= form_for(@product, :html => {class: "form-horizontal col-sm-12"}) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, class: "col-sm-2" %>
    <%= f.text_field :name, class: "col-sm-10" %>
  </div>

  <div class="form-group">
    <%= f.label :type, class: "col-sm-2" %>
    <%= f.text_field :type, class: "col-sm-10" %>
  </div>

  <div class="form-group">
    <%= f.label :quantity, class: "col-sm-2" %>
    <%= f.text_field :quantity, class: "col-sm-10" %>
  </div>

  <div class="actions">
    <%= f.submit "Create Product", class: 'btn btn-lg btn-default pull-right' %>
  </div>
<% end %>
